import { MostrarContrasenaPipe } from './mostrar-contrasena.pipe';

describe('MostrarContrasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarContrasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
